<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahmadsheraz.worldanimecommunity">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activites.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activites.signIn" />
        <activity android:name=".Activites.signup" />
        <activity
            android:name=".Activites.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activites.newPostActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activites.chatRoom"
            android:theme="@style/AppTheme.NoActionBar">
            >
        </activity>
        <activity
            android:name=".Activites.chatActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activites.CreateChatRoom"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".Activites.profile"
            android:theme="@style/AppTheme.NoActionBar">


        </activity>
    </application>

</manifest>